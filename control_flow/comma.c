#include<stdio.h>

int main(){
    // example - multiple variable declaration
    int x, y = 10;
    while( i < 100 ){
        sum += i, ++i; // reduce lines of code
    }

    // example 2
    int i (5, 10); // 10 is assigned to the variable i

    // example 3 - binary function
    int j = (fi(), foo());

    // example 4
    x = (y = 3, (z = ++y + 2) + 5);
    
     // example 5
    printf("%d = %d", one , two);

     // example 6
    void func(x, y);
    return 0;
}